
{TODO}
Program Quicksort.pas;
Var
n,i,j: Integer;
ls, rs, arr: Array of Integer;
Begin
    write('Geben Sie n ein: ');
    readln(n);
    setLength(arr,n);
    For i:=0 to n-1 do
        Begin
            write('=> ');
            readln(arr[i]);
        End;


End.
